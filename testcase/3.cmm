int sqrt(int x) {
	int l = 0, r = x;
	while(l != r) {
		int m = (l+r) / 2 + 1;
		if(m * m > x) r = m - 1; else l = m;
	}
	return l;
}

int main() {
	int x = read(), y = sqrt(x);
	write(y);
	return 0;
}
